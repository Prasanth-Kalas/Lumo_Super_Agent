{
  "_comment": "Production overlay — set LUMO_REGISTRY_PATH=/app/config/agents.registry.prod.json in the orchestrator deployment. Base URLs must point at the agent's public hostname (behind CloudFront/ALB). version pins MUST be exact in prod.",
  "env": "prod",
  "agents": [
    {
      "key": "lumo-ml",
      "enabled": false,
      "system": true,
      "base_url": "https://ml.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Flip enabled=true after Lumo_ML_Service is deployed on Cloud Run and LUMO_ML_SERVICE_JWT_SECRET is set in both Super Agent and the service."
    },
    {
      "key": "food",
      "enabled": true,
      "base_url": "https://food.agents.lumo.rentals",
      "version": "0.1.0"
    },
    {
      "key": "flight",
      "enabled": false,
      "base_url": "https://flight.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Flip enabled=true after Duffel live mode + Seller of Travel compliance review."
    },
    {
      "key": "restaurant",
      "enabled": false,
      "base_url": "https://restaurant.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Enabled=false until OpenTable affiliate approval. The stub works in dev; prod should not ship user-facing 'book a reservation' traffic against mock data. Flip to true once OPENTABLE_PARTNER_ID+OPENTABLE_API_KEY are set on the restaurant agent's prod env and opentable-real.ts is fully wired."
    },
    {
      "key": "hotel",
      "enabled": false,
      "base_url": "https://hotel.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Enabled=false on prod until amadeus-real.ts is implemented and promoted from test.api.amadeus.com to api.amadeus.com. The stub is fine in dev/vercel preview; prod should not ship real 'book a hotel' traffic against an in-memory catalog. Flip to true once AMADEUS_CLIENT_ID+AMADEUS_CLIENT_SECRET are set on the hotel agent's prod env AND the real client carries search/availability/create/cancel."
    },
    {
      "key": "open-weather",
      "enabled": false,
      "base_url": "https://open-weather.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Open Weather Agent. Read-only public data (Open-Meteo + NWS) — safe to flip true as soon as the agent is deployed at this hostname."
    },
    {
      "key": "open-maps",
      "enabled": false,
      "base_url": "https://open-maps.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Open Maps Agent (Nominatim + OSRM). Flip to true once deployed."
    },
    {
      "key": "open-ev-charging",
      "enabled": false,
      "base_url": "https://open-ev-charging.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Open EV Charging Agent. Needs OPENCHARGEMAP_API_KEY on the agent's prod env. Flip to true once deployed."
    },
    {
      "key": "open-events",
      "enabled": false,
      "base_url": "https://open-events.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Open Events Agent. Wikidata works without auth; optional TICKETMASTER_API_KEY on the agent's prod env. Flip to true once deployed."
    },
    {
      "key": "open-attractions",
      "enabled": false,
      "base_url": "https://open-attractions.agents.lumo.rentals",
      "version": "0.1.0",
      "_notes": "Open Attractions Agent (Nominatim + Overpass). Flip to true once deployed."
    }
  ]
}
